@using MusicOrganizer.Models;

<h1></h1>

@if (Model.Count == 0)
{
  <p>There are no items in the list.</p>
}
<ul>
  @foreach (Disk disk in Model)
  {
    <li><a href="/disks/@disk.Id">@disk.Description</a></li>
  }
</ul>